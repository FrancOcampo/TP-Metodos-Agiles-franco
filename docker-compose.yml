version: "3.8"

services:
  app:
    build: .
    container_name: gestion-licencias-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    driver: bridge

